# Instalar MailHog (una sola vez):
# 1. Descarga desde: https://github.com/mailhog/MailHog/releases
# 2. O instala con Chocolatey: choco install mailhog
# 3. O con Scoop: scoop install mailhog

# Ejecutar MailHog:
# mailhog

# Configuración para .env:
MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="security@tuapp.com"
MAIL_FROM_NAME="Sistema de Seguridad"

# Ver emails en: http://localhost:8025